tr
  td #{item.value._id}
  td #{item.value.year}
  td
    - for (var i = 0; i < item.value.actors.length - 1; i = i + 1)
      #{item.value.actors[i]}, 
    #{item.value.actors[i]}
  td
    a(href='/delete/' + item.value._id + '/' + item.value._rev) Delete